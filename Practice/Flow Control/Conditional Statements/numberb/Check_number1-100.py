# Program to check whether the number is between 1-100 or not
number = eval(input('Enter The Number: '))
if number >=1 and number<=100:
  print(f'The number:{number} is IN Between 1 to 100')
else:
  print(f'The number:{number} is NOT IN Between 1 to 100')
