#Program to Find Area of a Circle
# area = pi*r**2, where r= circle radius

# Get input data from user
from math import *
radius=eval(input('Enter radius value:'))
area=pi*(radius**2)
print(f'Area of circle is={area}')
