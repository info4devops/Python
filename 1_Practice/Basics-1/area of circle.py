# Python program that calculates the area of a circle based on the radius entered by the user.

from math import*
r = eval(input('Enter area of circle: '))
area = pi*r**2
print(f'Area of circle with radius:{r} is ={area}')
